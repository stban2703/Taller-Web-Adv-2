import React from 'react';
import { HashRouter } from 'react-router-dom';

export const App = () => {
    return (
        <main>
            <HashRouter>
                
            </HashRouter>
        </main>
    )
}